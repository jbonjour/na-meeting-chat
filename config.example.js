// Copy this file to config.js and fill in your values.
// NEVER commit config.js to git — your API key must stay private.

const CONFIG = {
  GEMINI_API_KEY: "your-gemini-api-key-here",

  // Your area name — shown in the header and welcome message
  AREA_NAME: "Portland NA",   // e.g. "Portland NA" or "Contra Costa NA"

  // BMLT root server for your region
  BMLT_ROOT_SERVER: "https://bmlt.wszf.org/main_server",

  // Service body IDs:  Portland NA = 39,  Contra Costa NA = 26
  DEFAULT_SERVICE_BODY_ID: 39
};
